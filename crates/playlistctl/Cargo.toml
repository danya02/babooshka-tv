[package]
name = "playlistctl"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.54", features = ["derive"] }
crossterm = "0.29"
ratatui = { version = "0.30", default-features = false, features = ["all-widgets", "crossterm_0_29", "layout-cache", "underline-color"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
